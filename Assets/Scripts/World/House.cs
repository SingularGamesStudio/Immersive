using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class House : MonoBehaviour
{
    List<DestObject> FoodStorage = new List<DestObject>();
    List<DestObject> BooksStorage = new List<DestObject>();
    List<DestObject> StuffStorage = new List<DestObject>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
